signature LOCK_STEP = sig

   (* Lock the piece at position `(px, py)` and angle `a` *)
(*  datatype step = Step of { px: int,
                            py: int,
                            a: int,
                            state: Board.state
                          } *)

end
